header.header 
    .container-fluid
        .row.align-items-center
            .col-3 
                .inner-logo 
                    a(href="/admin/dashbroad") ADMIN
            .col-9 
                div(class="text-right")
                    a(href=`${prefixAdmin}/my-account`
                        class="btn btn-sm btn-success"
                    ) #{user.fullName}
                    a(href=`${prefixAdmin}/auth/logout`
                        class="btn btn-sm btn-danger"
                    ) Đăng xuất
